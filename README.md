# RAG_with_milvus
A lightweight, high-performance Retrieval-Augmented Generation (RAG) insertion and extraction engine built natively in Python. This implementation intentionally cuts out framework overhead like LangChain or LangGraph, offering absolute control over parsing, local embedding execution, and vector storage.
